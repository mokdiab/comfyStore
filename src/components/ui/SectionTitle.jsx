const SectionTitle = ({ text }) => {
  return (
    <div className="pb-5 border-b border-base-300">
      <h1 className="capitalize font-medium text-3xl tracking-wider">{text}</h1>
    </div>
  );
};

export default SectionTitle;
